export default `/**
 * Sums two binary strings and returns the result as a binary string.
 * @param {string} strA - The first binary string.
 * @param {string} strB - The second binary string.
 * @returns {string} The sum of the two binary strings.
 */
function binarySum(strA, strB) {
  // your code below
  

  
  // your code above
}`;