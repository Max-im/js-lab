export default `/**
 * Finds the character that appears the most in the given string.
 * @param {string} str - The string to analyze.
 * @returns {string} The character that appears the most.
 */
function maxChar(str) {
    // your code below
    
    
  
    
    // your code above
  }`;